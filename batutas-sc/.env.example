# Throwaway dev wallet private key — NEVER use a wallet holding real funds.
PRIVATE_KEY=your_dev_wallet_private_key

# Celoscan API key — required for contract verification.
CELOSCAN_API_KEY=your_celoscan_api_key
