{
  "name": "batutas-subgraph",
  "version": "0.1.0",
  "description": "Subgraph indexing BATUTAS game events on Celo Mainnet",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy batutas",
    "create-local": "graph create --node http://localhost:8020/ batutas",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 batutas"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.80.0",
    "@graphprotocol/graph-ts": "^0.35.0"
  }
}
